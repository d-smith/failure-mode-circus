1. Read `baseline-plan.md` and `progress.txt` thoroughly to understand the project state.
2. Identify the single, highest-leverage unchecked task from the checklist.
3. Implement the feature or fix cleanly.
4. Run the project's test suite and linter to self-validate your changes.
5. Update `progress.txt` and check off the completed item in `baseline-plan.md`.
6. If all tasks in the plan are fully checked off and passing validation, output the exact string: `<<PROMISE>COMPLETE</PROMISE>`. Otherwise, exit cleanly.
